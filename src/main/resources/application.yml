server:
  port: 83



spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/db1?serverTimezone=UTC
      username: root
      password: root
mybatis-plus:
  global-config:
    db-config:
      id-type: auto #Mp id自增策略
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #Mp日志
